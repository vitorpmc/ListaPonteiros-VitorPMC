#include <stdio.h>
#include <stdlib.h>

// Realização de soma 
void soma(int *a, int *b, int *c, int q){
  for(int k = 0; k < q; k++){
    c[k] = a[k] + b[k];
  }
  printf("O vetor soma eh: \n");
  for (int i = 0; i < q; i++){
    
  }

}